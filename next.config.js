/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = {
    nextConfig,
    images: {
        domains: ['media.istockphoto.com', 'example.com', 'images.unsplash.com'], // Add 'images.unsplash.com' to the list
    },
    }
 
